apiVersion: v1
kind: Service
metadata:
  name: gitops-microservice-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: gitops-microservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
